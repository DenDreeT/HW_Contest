/*
Задание_A4_контеста
 */

#include <stdio.h>

int a,b,c,sum,res; 
int main (void) {
	scanf("%d %d %d", &a, &b, &c);
    printf("%d+%d+%d=%d\n", a, b, c, sum=a+b+c);
    printf("%d*%d*%d=%d\n", a, b, c, res=a*b*c);

return 0;
}
