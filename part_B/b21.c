/*
 *  задание_B21_контеста
 * Преобразование заглавных символов в строчные
  */

#include <stdio.h>

int main() {
    char c;
    while ((c = getchar())!='.') { // Читаем один символ

    // Проверяем, является ли символ заглавной буквой
		if (c >= 'A' && c <= 'Z') {
        // Преобразуем в строчную букву
        c = c + ('a' - 'A'); // или c = c - 32;
        }
		putchar(c); // Выводим результат
	}
    return 0;
}

