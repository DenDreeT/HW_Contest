/*
 *  задание_B15_контеста
 * Подсчет количества четных чисел в строке до знака '0'
  */

#include <stdio.h>

int main() {
	
    char c;
    int num=0, cnt=0;
    while( (c=getchar())!='0') { //символ-ограничитель вводимых данных
    
 /*       if(c>='0' && c<='9')
            num = num*10+c-'0';	//перевод символа в число
*/            
        if (c == ' ') cnt++;	//счетчик пробелов
    }
    printf("%d\n", cnt);
    return 0;
}
